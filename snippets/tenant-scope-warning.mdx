<Warning>
**Multi-Tenant Data Isolation**

All API calls are automatically scoped to your authenticated tenant. You cannot access data from other law firms. If you need to override tenant context (e.g., for webhooks), use the `x-tenant-id` header.
</Warning>
